{
  "//": "auspex Cursor cloud capture — PRIMARY recipe config (no custom image). PER-REPOSITORY form:",
  "//0": "commit ONLY this file into your repo's .cursor/ — no recipe scripts are vendored. install/start",
  "//1": "fetch the cosign-signed cursor-cloud-{install,start}.sh Release assets, so an upstream script fix",
  "//1a": "flows in automatically (nothing to keep in sync). It's the same commands as the team-level form",
  "//1b": "(README.md Option A), just committed to the repo so they override the team environment. Cursor",
  "//2": "reads .cursor/environment.json from the launch branch; launch a NORMAL agent (the interactive",
  "//2a": "'Set up agent' button is a different mode).",
  "//secrets": "Set AUSPEX_CLOUD_TOKEN as a Runtime Secret (team-scoped). Set AUSPEX_BASE_URL (+ optional",
  "//secrets2": "AUSPEX_VERSION / AUSPEX_VERIFY) as environment variables. The work email is auto-discovered —",
  "//secrets3": "no per-user secret needed. Egress allowlist + full walkthrough: see README.md.",
  "//phases": "install runs during Build (post-clone, root/sudo-capable); start runs at each agent run.",
  "//pin": "latest/download tracks the newest release; swap in .../releases/download/<tag>/… to pin.",
  "install": "curl -fsSL https://github.com/Attuned-Corp/auspex-distribution/releases/latest/download/cursor-cloud-install.sh | bash",
  "start": "curl -fsSL https://github.com/Attuned-Corp/auspex-distribution/releases/latest/download/cursor-cloud-start.sh | bash"
}
